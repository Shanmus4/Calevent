/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // experimental.allowedDevOrigins removed due to unsupported option in Next.js 15.3.1
}

module.exports = nextConfig;
